import { Card, CardHeader, CardTitle, CardContent } from "@/components/ui/card";

export default function AirtimeDataPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Airtime & Data</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Top-up options for MTN, Orange, and Nexttel will be here.</p>
      </CardContent>
    </Card>
  );
}
